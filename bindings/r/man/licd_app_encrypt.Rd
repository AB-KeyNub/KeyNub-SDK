% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/records.R
\name{licd_app_encrypt}
\alias{licd_app_encrypt}
\alias{licd_app_decrypt}
\title{Encrypt data that only a dongle can decrypt}
\usage{
licd_app_encrypt(dongle, data, scope = c("device", "developer"))

licd_app_decrypt(dongle, packed)
}
\arguments{
\item{dongle}{A \code{licd_dongle} with an open session.}

\item{data}{The plaintext: a raw vector, or a single string for its bytes.}

\item{scope}{\code{"device"} or \code{"developer"}.}

\item{packed}{The sealed data from \code{licd_app_encrypt()}, a raw vector.}
}
\value{
A raw vector: the sealed data, or the recovered plaintext.
}
\description{
The pair to build a licence check on. \code{licd_app_encrypt()} seals \code{data} so
that only a dongle can open it; \code{licd_app_decrypt()} opens it again. Put
something the program genuinely needs through this, such as the parameters
of your model, and ship only the sealed form: then removing the check
removes the data. Bulk encryption runs on the host; the dongle wraps only a
small key.
}
\details{
\code{scope = "developer"} lets any dongle you have issued decrypt the data, so
one sealed file serves every customer; \code{scope = "device"} locks it to the
one dongle that sealed it.
}
\examples{
\dontrun{
sealed <- licd_with_session(dongle, licd_app_encrypt(dongle, "the data", "developer"))
saveRDS(sealed, "model-parameters.sealed")
# In the shipped program:
plain <- licd_with_session(dongle, licd_app_decrypt(dongle, readRDS("model-parameters.sealed")))
rawToChar(plain)
}
}
