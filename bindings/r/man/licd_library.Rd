% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/library.R
\name{licd_library}
\alias{licd_library}
\title{The native library in use}
\usage{
licd_library(path = NULL)
}
\arguments{
\item{path}{A file path to the library, or \code{NULL} to ask.}
}
\value{
The path in use, or the one that would be used: the loaded library
once there is one, otherwise the choice the next call would make.
Invisibly when \code{path} is given.
}
\description{
The package does its work through the KeyNub native library
(\code{keynub_licdongle.dll}, \code{libkeynub_licdongle.so} or
\code{libkeynub_licdongle.dylib}), which a process loads once, on the first call
that needs it. \code{licd_library(path)} names the file to load, and must come
before that first call. Without it the package takes, in this order, the
\code{KEYNUB_LICDONGLE_LIBRARY} environment variable, the \verb{natives/<platform>/}
folder of a clone of the SDK repository found from the working directory
upwards, and finally the bare file name, which the operating system resolves
along its usual search path (\code{PATH}, \code{LD_LIBRARY_PATH},
\code{DYLD_LIBRARY_PATH}).
}
\examples{
\dontrun{
licd_library("C:/keynub/natives/win-x64/keynub_licdongle.dll")
licd_version()
}
}
