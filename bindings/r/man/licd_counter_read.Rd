% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/records.R
\name{licd_counter_read}
\alias{licd_counter_read}
\alias{licd_counter_increment}
\title{Monotonic counters}
\usage{
licd_counter_read(dongle, id)

licd_counter_increment(dongle, id)
}
\arguments{
\item{dongle}{A \code{licd_dongle} with an open session.}

\item{id}{The counter, an integer from 0 upwards; the dongle reports
\code{licd_status_codes[["range"]]} for one it does not have.}
}
\value{
The counter's value (after the increment, for
\code{licd_counter_increment()}) as a number. Counters are unsigned 32-bit, so
the value is returned as a double.
}
\description{
The dongle holds hardware counters that only ever go up. Reading needs a
session; incrementing needs the write role and is irreversible.
}
\examples{
\dontrun{
licd_with_session(dongle, licd_counter_read(dongle, 0))
}
}
