% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dongle.R
\name{licd_verify_genuine}
\alias{licd_verify_genuine}
\alias{licd_is_genuine}
\title{Prove that a dongle is genuine}
\usage{
licd_verify_genuine(dongle)

licd_is_genuine(dongle)
}
\arguments{
\item{dongle}{A \code{licd_dongle}.}
}
\value{
\code{licd_verify_genuine()}: a list with \code{genuine} (\code{TRUE}), \code{serial}
(from the verified certificate) and \code{provisioned_date} (\code{"YYYY-MM-DD"}, or
\code{""}). \code{licd_is_genuine()}: \code{TRUE} or \code{FALSE}.
}
\description{
Verifies the dongle's certificate chain against the trusted root and runs a
live challenge-response against the key inside the dongle.
\code{licd_verify_genuine()} signals a condition of class \code{licd_not_genuine} (or
\code{licd_certificate_invalid}) when the proof fails; \code{licd_is_genuine()} is the
non-signalling form for a gate and \strong{fails closed}: a missing dongle, an I/O
error and an invalid certificate all give \code{FALSE}.
}
\details{
Read the SDK's integration security guide before building a check on the
answer. \code{if (!licd_is_genuine(dongle)) stop()} is one line to delete from a
program that ships as source; what cannot be deleted is data the program
needs and only the dongle can decrypt, see \code{\link[=licd_app_encrypt]{licd_app_encrypt()}}.
}
\examples{
\dontrun{
result <- licd_verify_genuine(dongle)
result$serial
}
}
