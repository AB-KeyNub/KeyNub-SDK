% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/records.R
\name{licd_record_read}
\alias{licd_record_read}
\alias{licd_record_write}
\alias{licd_record_erase}
\alias{licd_record_erase_all}
\title{Read, write and erase records}
\usage{
licd_record_read(dongle, name, progress = NULL)

licd_record_write(dongle, name, data, progress = NULL)

licd_record_erase(dongle, name)

licd_record_erase_all(dongle)
}
\arguments{
\item{dongle}{A \code{licd_dongle} with an open session.}

\item{name}{The record name, a non-empty string.}

\item{progress}{\code{NULL}, or a \verb{function(done, total)} called as the transfer
proceeds, both arguments in bytes. Return \code{FALSE} to cancel, which signals
\code{licd_cancelled}; an error inside the function cancels as well.}

\item{data}{The bytes to store: a raw vector, or a single string for its
bytes.}
}
\value{
\code{licd_record_read()}: the record as a raw vector (\code{rawToChar()} gives
text back). The others: \code{NULL}, invisibly.
}
\description{
Records are named blobs in the dongle's storage; a licence typically is one.
Reading needs a session, writing and erasing need the write role
(\code{\link[=licd_write_auth]{licd_write_auth()}}). \code{licd_record_write()} replaces the record atomically.
A record that does not exist signals \code{licd_not_found}.
}
\details{
\code{licd_record_erase_all()} is a separate function on purpose: to the C
library a missing name means "erase every record", and an accidentally empty
variable must not do that.
}
\examples{
\dontrun{
licd_with_session(dongle, {
  licence <- rawToChar(licd_record_read(dongle, "license"))
})
}
}
