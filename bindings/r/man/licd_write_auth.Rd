% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dongle.R
\name{licd_write_auth}
\alias{licd_write_auth}
\alias{licd_write_auth_rotate}
\title{Elevate to the write role}
\usage{
licd_write_auth(dongle, key)

licd_write_auth_rotate(dongle, key)
}
\arguments{
\item{dongle}{A \code{licd_dongle} with an open session.}

\item{key}{The key as a raw vector, for example
\code{readBin(path, "raw", file.size(path))}.}
}
\value{
\code{NULL}, invisibly.
}
\description{
\code{licd_write_auth()} unlocks writing, erasing and counter increments for the
rest of the session with the dongle's write key, a P-256 private key in
PKCS#8 DER. This belongs in your licence-issuing tooling; never ship that key
in the application your users run. A key the dongle does not accept signals
\code{licd_not_genuine}.
}
\details{
\code{licd_write_auth_rotate()} replaces the dongle's write key with \code{key}, a key
you hold. It needs the write role, so call \code{licd_write_auth()} with the
current key first. That session keeps the write role; from the next session
on only the new key elevates, and the old one no longer works on this
dongle. Do this once per dongle, when it arrives: the factory key is public.
}
\examples{
\dontrun{
key <- readBin("my-write-key.der", "raw", file.size("my-write-key.der"))
licd_with_session(dongle, {
  licd_write_auth(dongle, key)
  licd_record_write(dongle, "license", "expires 2027-12-31")
})
}
}
