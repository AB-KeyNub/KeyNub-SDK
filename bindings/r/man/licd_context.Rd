% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context.R
\name{licd_context}
\alias{licd_context}
\title{Create a library context}
\usage{
licd_context()
}
\value{
A \code{licd_context} object.
}
\description{
A context owns the connection to the operating system's USB layer and the
dongles opened on it. One per program is usual. Close it with
\code{\link[=licd_close]{licd_close()}} when done; a context the garbage collector reclaims is closed
as well, together with any dongle still open on it.
}
\examples{
\dontrun{
ctx <- licd_context()
licd_enumerate(ctx)
licd_close(ctx)
}
}
