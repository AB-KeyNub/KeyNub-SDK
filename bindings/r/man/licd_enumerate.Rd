% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context.R
\name{licd_enumerate}
\alias{licd_enumerate}
\title{List the attached dongles}
\usage{
licd_enumerate(ctx)
}
\arguments{
\item{ctx}{A \code{licd_context}.}
}
\value{
A data frame with one row per dongle and the columns \code{serial} (hex),
\code{path} (the operating system's device path, accepted by
\code{\link[=licd_open_path]{licd_open_path()}}), \code{vendor_id} and \code{product_id} (USB ids). Zero rows when
none is attached.
}
\description{
Enumerates without opening anything.
}
\examples{
\dontrun{
ctx <- licd_context()
dongles <- licd_enumerate(ctx)
nrow(dongles)
}
}
