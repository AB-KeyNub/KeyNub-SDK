% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/errors.R
\name{licd_error}
\alias{licd_error}
\title{Errors signalled by this package}
\description{
Every failure the native library reports is signalled as a condition of
class \code{licd_error} (and \code{error}), with these fields:
}
\details{
\itemize{
\item \code{status}: the code, one of \link{licd_status_codes};
\item \code{operation}: the SDK function that failed, such as \code{"licd_open"};
\item \code{detail}: the library's diagnostic text for this failure, or \code{""}. Log it;
do not parse it.
}

The failures a program is likely to branch on carry a more specific class in
front of \code{licd_error}: \code{licd_no_device}, \code{licd_not_genuine},
\code{licd_certificate_invalid}, \code{licd_session_expired}, \code{licd_not_found},
\code{licd_auth_required} and \code{licd_cancelled}.
}
\examples{
\dontrun{
ctx <- licd_context()
dongle <- tryCatch(licd_open(ctx),
  licd_no_device = function(e) NULL)
}
}
