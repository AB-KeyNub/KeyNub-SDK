% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dongle.R
\name{licd_info}
\alias{licd_info}
\title{Plaintext device information}
\usage{
licd_info(dongle)
}
\arguments{
\item{dongle}{A \code{licd_dongle}.}
}
\value{
A list: \code{protocol_version} (two integers), \code{firmware_version} (three
integers), \code{se_ready} (the secure element responded), \code{provisioned}
(factory provisioning complete), \code{data_capacity} and \code{data_free} (bytes),
\code{watchdog_reboot}, \code{isolated} and \code{writeauth_rotated} (logicals).

\code{watchdog_reboot} means the dongle's \emph{previous} boot ended in a watchdog
timeout: the firmware hung and reset itself. It is the only trace a field
hang leaves behind and a power cycle clears it, so it is worth logging.

\code{writeauth_rotated} is \code{FALSE} while the dongle still accepts the write key
it left the factory with. That key is public, so a dongle in that state
takes writes from anyone holding it; rotate on receipt with
\code{\link[=licd_write_auth_rotate]{licd_write_auth_rotate()}}.
}
\description{
Reads what the dongle reports without a session.
}
\examples{
\dontrun{
info <- licd_info(dongle)
info$firmware_version
}
}
