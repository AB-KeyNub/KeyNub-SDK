% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context.R
\name{licd_open}
\alias{licd_open}
\alias{licd_open_path}
\title{Open a dongle}
\usage{
licd_open(ctx, serial = NULL)

licd_open_path(ctx, path)
}
\arguments{
\item{ctx}{A \code{licd_context}.}

\item{serial}{The serial as hex, or \code{NULL} for the first dongle.}

\item{path}{A device path from \code{\link[=licd_enumerate]{licd_enumerate()}}.}
}
\value{
A \code{licd_dongle} object. Close it with \code{\link[=licd_close]{licd_close()}}.
}
\description{
\code{licd_open()} opens the dongle with the given serial, or the first one found
when \code{serial} is \code{NULL}. \code{licd_open_path()} opens the dongle at a device path
from \code{\link[=licd_enumerate]{licd_enumerate()}}. Both signal a condition of class \code{licd_no_device}
when there is no such dongle.
}
\examples{
\dontrun{
ctx <- licd_context()
dongle <- licd_open(ctx)             # the first dongle
licd_verify_genuine(dongle)          # signals an error unless genuine
licd_close(dongle)
licd_close(ctx)
}
}
