% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dongle.R
\name{licd_session_open}
\alias{licd_session_open}
\alias{licd_session_close}
\alias{licd_with_session}
\title{Open and close the encrypted session}
\usage{
licd_session_open(dongle)

licd_session_close(dongle)

licd_with_session(dongle, expr)
}
\arguments{
\item{dongle}{A \code{licd_dongle}.}

\item{expr}{An expression to evaluate while the session is open.}
}
\value{
\code{licd_session_open()} and \code{licd_session_close()}: \code{NULL}, invisibly.
\code{licd_with_session()}: the value of \code{expr}.
}
\description{
Records, counters and app-data encryption need a session: an encrypted,
authenticated channel to the dongle (P-256 ECDH, HKDF-SHA256, AES-256-GCM).
\code{licd_with_session()} opens one, evaluates \code{expr} and closes the session
afterwards whatever happens.
}
\examples{
\dontrun{
data <- licd_with_session(dongle, licd_record_read(dongle, "license"))
}
}
